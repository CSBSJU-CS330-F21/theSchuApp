import React, {useState} from 'react';
import { StyleSheet, View, Text, SafeAreaView,Button, Image, ScrollView, FlatList } from 'react-native';
import firebase from './database/firebase-db'
var id = 0;
var count = 0;
const drinks = [];

export default [
{
  id: 1,
}

];









