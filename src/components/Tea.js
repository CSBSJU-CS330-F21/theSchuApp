import React from 'react';
import { StyleSheet, View, Text, SafeAreaView, Image, ScrollView } from 'react-native';

export default function Tea() {
    return (
     <Text>Tea Component</Text>
    );
  }